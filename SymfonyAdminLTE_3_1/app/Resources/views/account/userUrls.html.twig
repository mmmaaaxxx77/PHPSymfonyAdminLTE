<script>
    _URLS = {
        "ALL_USER" : "{{ path('allUser', {'page': '_page_', 'size': '_size_'}) }}",
        "REGISTER_USER" : "{{ path('registerUser') }}",
        "UPDATE_USER_ACTIVE" : "{{ path('updateUserActive') }}",
        "DELETE_USER" : "{{ path('deleteUser') }}",
        "EDIT_USER" : "{{ path('editUser', {'userId': '_userId_'}) }}"
    }

</script>